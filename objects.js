const playlist = new Object({ Nero: 'Promises'})

function updatePlaylist(playlist, artistName, songTitle){
  return Object.assign({}, 'playlist', {['Pendulum']: 'Tarantula'})
  return updatePlaylist
}